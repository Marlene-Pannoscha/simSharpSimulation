# Ignore everything by default.
*

# Keep directory traversal enabled so nested .cs files can be tracked.
!*/

# Track only source files.
!*.cs
!.gitignore
!.txt
!.md

# Never track generated build output, even if it contains generated .cs files.
**/bin/**
**/obj/**
